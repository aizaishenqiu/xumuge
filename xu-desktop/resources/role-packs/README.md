# Role packs (open-source)

Source Markdown: `../role-packs-src/zh-virmoon` (~461 non-market demo roles).

Built binary `full.zh-CN.xupack` is produced by `pnpm build:release:oss-unsigned`
(or `pnpm build:role-pack:full`) and is **gitignored**.

Commercial `zh-CN-virmoor` / market roles are **not** included.
